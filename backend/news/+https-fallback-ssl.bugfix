Stop verifying TLS certificates when checking external links, so that an unverifiable certificate no longer defeats the `http://` -> `https://` retry. A link broken over http that works over https is now reported as "update to https" even when the https server omits its intermediate certificate (browsers and curl recover the missing intermediate, `requests`/`certifi` do not, which made such links look broken).
